# Check for projects within other project names

Check for projects within other project names

## Usage

``` r
within_match(a, b)
```

## Arguments

- a:

  vector holding names that may be a part of `b`

- b:

  vector holding names that may encompass `a`
