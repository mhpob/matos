# Use `agrep` for fuzzy matching

Use `agrep` for fuzzy matching

## Usage

``` r
fuzzy_match(a, b)
```

## Arguments

- a:

  vector holding names that may be a part of `b` by changing less than
  25% of the characters

- b:

  vector holding names that may encompass some manipulation of `a`
